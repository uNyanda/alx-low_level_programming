#include "main.h"

/**
 * _abs - This function prints the absolute value of n
 * @n: The number to be printed
 * Return: absolute value
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
